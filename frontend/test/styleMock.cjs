// Stub for stylesheet imports (e.g. `import './Component.scss'`) so Jest does
// not try to parse SCSS. Components import their own styles for encapsulation;
// styles are irrelevant to the unit tests, so an empty module is sufficient.
module.exports = {};
